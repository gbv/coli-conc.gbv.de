---
layout: layouts/base.njk
---
{% set title = title | default(strings.pages[page.fileSlug]) | default(page.fileSlug) %}

{% div "text-center" %}

# {{ title | localize }}
{{ subtitle | localize }}

{% enddiv %}

{{ content | safe }}
